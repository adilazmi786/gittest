Generate SSH key using=>  ssh-keygen -t rsa -b 4096 -C "email id".
Copy the output using => cat ~/.ssh/id_rsa.pub
Paste the above copied outpur into your Github profile -> Settings ->
SSH and GPG Keys -> Add new SSH key.
